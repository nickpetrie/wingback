# Anon key + URL only. The service-role key belongs in Supabase function
# secrets (`supabase secrets set ...`) — never here, never behind a
# NEXT_PUBLIC_ prefix.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
